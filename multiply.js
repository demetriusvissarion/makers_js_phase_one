const multiply = (a, b) => {
    return a * b
};

// console.log(multiply(2, 2));

module.exports = multiply;

/////// for REPR
// const multiply = require('./multiply');
// console.log(multiply(2,2));